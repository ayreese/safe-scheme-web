export interface Feature {
    title: string;
    description: string;
    copy: string;
    isBeta: boolean;
    image: string
}

export interface Testimonial {
    name: string,
    role: string,
    testimonial: string,
    image: string,
}